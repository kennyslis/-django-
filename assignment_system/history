先看看是否已经提交：

git status

如果看到：

nothing to commit, working tree clean

说明已经 commit，可以直接 push。

如果还有文件没提交：

git add .
git commit -m "你的提交说明"

###################
如果你在 main 分支：

git push origin main

如果是第一次推送，建议用：

git push -u origin main

-u 的作用是以后可以直接用：

git push
###########